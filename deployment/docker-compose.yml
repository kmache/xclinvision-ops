services:
  backend:
    build: ./app/backend
  frontend:
    build: ./app/frontend
  nginx:
    image: nginx:latest
    volumes:
      - ./deployment/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80" 
    depends_on:
      - backend
      - frontend